
directive lean
import Soda.tiles.fairness.tool.TileMessage
import Soda.tiles.fairness.tile.AllEqual1Tile

/**
 * This is a composite tile for the group fairness pipeline.
 */

class CrossFilterNotPLengthTile

  abstract
    protected_attribute : Agent -> Boolean

  not_protected_attribute (a : Agent) : Boolean =
    not (protected_attribute (a) )

  other_tile = CrossFilterPLengthTile .mk (not_protected_attribute)

  apply (message0 : TileMessage [Seq [Agent] ] )  (message1 : TileMessage [Seq [Resource] ] )
      : TileMessage [Measure] =
    other_tile .apply (
      message0
    ) (
      message1
    )

end

